# Do not edit this file directly. Use a 'docker-compose.override.yaml' file if you can.
# Refer to `docker-compose.override.yaml.example' for some sample configurations.

services:
  api:
    container_name: LibreChat
    ports:
      - "${PORT}:${PORT}"
    depends_on:
      - mongodb
      - rag_api
    image: ghcr.io/danny-avila/librechat-dev:latest
    restart: always
    user: "${UID}:${GID}"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - HOST=0.0.0.0
      - MONGO_URI=mongodb://mongodb:27017/LibreChat
      - MEILI_HOST=http://meilisearch:7700
      - RAG_PORT=${RAG_PORT:-8000}
      - RAG_API_URL=http://rag_api:${RAG_PORT:-8000}
    volumes:
      - type: bind
        source: ./.env
        target: /app/.env
      - ./images:/app/client/public/images
      - ./uploads:/app/uploads
      - ./logs:/app/api/logs
  mongodb:
    container_name: chat-mongodb
    image: mongo
    restart: always
    user: "${UID}:${GID}"
    ports:
      - "27017:27017"
    volumes:
      - ./data-node:/data/db
    command: mongod --noauth
  meilisearch:
    container_name: chat-meilisearch
    image: getmeili/meilisearch:v1.12.3
    restart: always
    user: "${UID}:${GID}"
    environment:
      - MEILI_HOST=http://meilisearch:7700
      - MEILI_NO_ANALYTICS=true
      - MEILI_MASTER_KEY=${MEILI_MASTER_KEY}
    volumes:
      - ./meili_data_v1.12:/meili_data
  vectordb:
    container_name: vectordb
    image: ankane/pgvector:latest
    environment:
      POSTGRES_DB: mydatabase
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    restart: always
    volumes:
      - pgdata2:/var/lib/postgresql/data
  rag_api:
    container_name: rag_api
    image: ghcr.io/danny-avila/librechat-rag-api-dev-lite:latest
    environment:
      - DB_HOST=vectordb
      - RAG_PORT=${RAG_PORT:-8000}
    restart: always
    depends_on:
      - vectordb
    env_file:
      - .env
  # google-sheet-mcp:
  #   build:
  #     context: ./google-sheet-mcp
  #     dockerfile: Dockerfile
  #   container_name: google-sheet-mcp
  #   restart: always
  #   environment:
  #     - GOOGLE_CLIENT_ID=162665597983-jb2jd8os89ac4rrspofs2kunicmjqmo6.apps.googleusercontent.com
  #     - GOOGLE_CLIENT_SECRET=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
  #     - GOOGLE_REDIRECT_URI=http://localhost:8000/auth/callback
  #     - GOOGLE_SPREEDSHEET_SCOPE=https://www.googleapis.com/auth/spreadsheets,https://www.googleapis.com/auth/drive
  #     - GOOGLE_DRIVE_SCOPE=https://www.googleapis.com/auth/drive
  #     - MONGO_URI=mongodb://127.0.0.1:27017
  #   volumes:
  #     - ./google-sheet-mcp/app:/app/app
  #     - ./google-sheet-mcp/tokens:/app/tokens
  #   ports:
  #     - 8000:8000

  razorcopy-mcp:
    build:
      context: ./razorcopy-mcp
      dockerfile: Dockerfile
    container_name: razorcopy-mcp
    ports:
      - "3001:3001"  # Map host 3001 to container 3001 (adjust as needed)
    environment:
      # Add any required env vars here
      - PYTHON_BASE_URL=http://127.0.0.1:8000
      - PYTHON_HOST=http://127.0.0.1:8000
    restart: always

    
volumes:
  pgdata2:
